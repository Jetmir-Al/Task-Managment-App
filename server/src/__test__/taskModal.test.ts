import request from "supertest";
import app from "../app";

describe("Task Modal routes", () => {

    // it("should return 400 if invalid credencials", async () => {
    //     const res = await request(app)
    //         .post("/api/task/all")
    //         .send({
    //             name: "",
    //             email: "test@gmail.com",
    //             password: "123456"
    //         });

    //     expect(res.status).toBe(400);
    // });
});